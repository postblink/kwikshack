# Drizzle
DATABASE_URL=./data/kwikshack.db
# Optional submit key — when set, POST /api/builds requires x-kwikshack-key
# header to match. Leave unset for open local dev.
#KWIKSHACK_SUBMIT_KEY=change-me
